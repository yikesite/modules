<div class="posts">
  {% for post in page.children_sort_by_custom_date_descending %}
    <div class="post">
      <h2><a href="{{ post.url }}">{{ post.name }}</a></h2>
      <p>{{ post.custom_date }} &mdash; {{ post.body | strip_html | truncatewords: 60 }}</p>
      <a href="{{ post.url }}">&rarr;&nbsp;Read&nbsp;More</a>
    </div>
  {% endfor %}​
</div>